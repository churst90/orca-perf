# Orca OCR user extension -- package marker.
#
# The loader treats this directory as an extension because it
# contains manifest.toml. The empty __init__.py here lets normal
# Python tooling (linters, type checkers, IDEs) also recognize the
# directory as a package; it's not strictly required by the loader
# itself.
